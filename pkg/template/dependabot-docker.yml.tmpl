{{- range . }}
  - package-ecosystem: "docker"
    directory: "{{ .Directory -}}"
    schedule:
      interval: "{{ .Interval -}}"
      {{- if and .Day (eq .Interval "weekly") }}
      day: "{{ .Day -}}"
      {{- end }}
    groups:
      minor:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"
{{- end -}}
